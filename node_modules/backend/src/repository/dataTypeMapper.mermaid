flowchart TD

subgraph mapDbRow
  input[input data] --> type{type}

  type -- array --> array[map each element]
  array -- 各要素 --> type

  type -- object --> object[各keyをcamelCaseへ]
  object --> result

  type -- null --> null["undefinedへ（オプション機能）"]
  null --> result
end
